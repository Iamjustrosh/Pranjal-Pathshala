import UpcomingPage from "@/components/admin/UpcomingPage";
export default function AttendancePage() {
  return (
    <UpcomingPage
      title="Attendance"
      description="Class attendance, together in one place."
    >
      Attendance management will arrive in a later phase. Continue using the
      existing attendance workflow until the Supabase migration is ready.
    </UpcomingPage>
  );
}
